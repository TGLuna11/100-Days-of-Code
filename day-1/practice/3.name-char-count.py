# Using the len() function to count the number of characters in the user's name
name = input("What is your name? ")
length = len(name)
print("Hello, " + name + "!. \nYour name has " + str(length) + " characters.")
